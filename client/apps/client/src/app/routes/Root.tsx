import {Background} from "../components/Background"
import { Navbar } from "../components/navbar";

export const Root = () => {
    return(
        <div>
            <Navbar/>
            <Background/>
        </div>
        );
}
export default Root;